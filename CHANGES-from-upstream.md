# Changes from upstream RNode Firmware

This repository is **Stolo Node Firmware**, a fork of
[markqvist/RNode_Firmware](https://github.com/markqvist/RNode_Firmware),
licensed under the GNU General Public License v3.0 like its upstream. This
file is the running record of every deliberate difference from the pinned
upstream commit, kept so that a recipient of a Stolo Node can see exactly
what was changed and why (GPL-3.0 §5(a)), and so a rebase onto a newer
upstream is a merge of hooks rather than a merge of logic.

**Upstream pin:** `markqvist/RNode_Firmware` master
`d39339f8ecd5145b248c18bac7b6ea0f82faf85a` (reports firmware version 1.86;
identical to the `1.86` tag except for one README line). The `master` branch
of this repository is that commit, untouched. Stolo work lands on `main`.

Every modified upstream file carries a line of the form
`// Modified by Stolo Systems Inc., <date>` near its licence header; new
files carry a Stolo copyright header under the same licence.

## Changes

| Date | Area | Change | Why |
|---|---|---|---|
| 2026-09-14 | Repo | Added `CHANGES-from-upstream.md`, `STOLO.md`, and a CI workflow that builds the T-Beam Supreme target from the pinned source and publishes the binary with its SHA-256. | GPL kit §4 (reproducible build + release manifest) and the F0 gate: the fork must build the unchanged target before any Stolo change lands. No firmware source is modified by this entry. |
| 2026-09-14 | `Makefile` | Added `flash-stolo-tbeam_supreme` (PORT/STOLO_PY/STOLO_RNODECONF variables): the upstream `upload-tbeam_supreme` sequence for a macOS bench (`/dev/cu.usbmodem*`, an explicit Python, no console-partition rewrite). Marked `Modified by Stolo Systems Inc., 2026-09-14`. | The upstream recipes assume Linux paths. The console partition is left alone until F9 regenerates the console image from this fork's source. |
| 2026-09-14 | `Bluetooth.h` (ESP32 BLE path), `Stolo.h`, `Makefile` | With `-DSTOLO_BUILD=1` (`make firmware-stolo-tbeam_supreme`): the pairing window is 120 s (`STOLO_BT_PAIRING_TIMEOUT`) instead of 35 s; a radio with **no bonds** boots straight into pairing and its window never closes; the bonded link is kept after a successful pairing instead of being dropped 2 s later. Without the flag the build is upstream's. `Bluetooth.h` marked `Modified by Stolo Systems Inc., 2026-09-14`. | F3 of the Stolo Node plan. Upstream's self-expiring window closed before users found the phone's Bluetooth sheet, and a unit with no bonds had no way to be paired without a cable; the post-bond disconnect made pairing and connecting two user steps. Passkey (MITM) pairing is unchanged. |
| 2026-09-14 | `StoloStore.h` (new), `StoloProtocol.h` (new), `Stolo.h`, `Framing.h`, `Utilities.h`, `RNode_Firmware.ino`, `Bluetooth.h`, `Tools/stolo_node_tool.py` (new), `Documentation/stolo-control-protocol.md` (new) | With `-DSTOLO_BUILD=1`: a versioned NVS config store (node key pair, owner, pairing window; A/B slots + CRC; upstream EEPROM untouched); the Stolo Control Protocol over KISS `0x7A` — HELLO/AUTH/ENROLL/FORGET_OWNER, GET/SET radio with a per-model band clamp, GET/SET WiFi, GET/SET Bluetooth, faults; input-source tagging in `buffer_serial`; a session gate on legacy KISS mutations from unauthenticated WiFi guests; boot-time (button held at power-on) owner-enrollment window. Marked files carry `Modified by Stolo Systems Inc., 2026-09-14`. | Plan milestones F2 and the USB half of F4. Without the flag the build is upstream's. |
| 2026-09-19 | `StoloProtocol.h`, `StoloStore.h`, `Stolo.h`, `RNode_Firmware.ino`, `Bluetooth.h`, `Remote.h`, `Tools/stolo_node_tool.py`, `Documentation/stolo-control-protocol.md`, `Tests/host/` (new), `Makefile` (`test-host`) | **The authority contract** (decision 2026-09-19, after the 14-finding review): a Bluetooth bond or USB cable grants access, the owner key grants configuration authority. A real session object bound to one connection (ends on BLE disconnect, WiFi client close, source change, `CMD_LEAVE`/`CMD_RESET`, idle timeout, ownership change). Three roles — owner / compat (unowned + present host) / guest — enforced **default-deny on legacy KISS and SCP alike**, sensitive reads included (`CFG_READ`/`ROM_READ`, which carry the WiFi PSK, are refused to guests). One radio policy consulted by SCP, the legacy frequency/power handlers and `startRadio()`. SCP v2: AUTH signs node key, nonce, epoch and carrier; ENROLL proofs are fresh; SET_* parse-then-commit-then-apply with link-ending actions last and an applied-tags byte in replies. Store: commit-a-copy, an ownership epoch floor (a slot below it is a rollback, not a recovery), recovery state instead of re-minting an unowned identity on unreadable slots, bootloader entropy enabled around first-boot key generation. Host tests against the real dispatcher: 91 checks. `Remote.h` newly marked `Modified by Stolo Systems Inc., 2026-09-19`. | Review findings F1 (global authorization), F2 (bonded clients bypass, PSK dump), F3 (band clamp SCP-only), F7 (store rollback), F8 (mutate-before-validate), F9 (transcript binding), F10 (entropy). Software verified; device qualification pending (testing queue). |
| 2026-09-19 | Authority corrections C1–C11; parser, store, USB, EEPROM, transport, entropy, rescue, compat, tools, docs and library pins | Session resets abort frames/FIFO; payload/mutation role checks; checked slot → floor → activation protocol and ambiguous-store recovery; USB event hooks/30 s fallback and tool LEAVE; accepted SET replies with pending flags and checked per-byte EEPROM; reply/drain before link actions; boot-seeded runtime DRBG; explicit visible physical rescue; DEV_SIG/RESET classification; once-only compat proposal/build switch; USB/encrypted-BLE control admission; permanent host regressions and pinned libraries. | Software verified by host regression tests and ESP32 compilation; no device qualified claims in this correction round. USB process-close behavior, BLE notification delivery, entropy and flash interruption remain bench work. |
| 2026-09-19 | `BLESerial.h`, `BLESerial.cpp`, `Bluetooth.h`, `RNode_Firmware.ino`, `Makefile`, `Stolo.h`, `StoloProtocol.h`, `StoloControl.h` (new), `Tests/host/`, `Documentation/stolo-control-protocol.md`, `Documentation/scp-over-ble-review.md` (new) | **SCP over BLE:** add an unadvertised CTRL/EVENT service beside NUS (BULK reserved), encrypted/MITM characteristic and CCCD permissions, a bounded callback-to-loop queue, independent KISS parser and per-request EVENT sink with MTU chunking. Keep one BLE authority session across NUS/CTRL; defer BLE callback session teardown to the loop. Add SCP LEAVE (0x06) for control-only authority cleanup without truncating NUS data, retain NUS SCP compatibility, and record once-only factory compat as decided. Add production parser/GATT-method host regressions and the brief review/app handoff. | Configure a node over BLE while NUS carries radio traffic. Host tests and T-Beam Supreme compilation are software verification only; iPhone/device qualification pending. See [brief review and gated app handoff](Documentation/scp-over-ble-review.md). |

| 2026-09-19 | OLED branding, F8, AP default and console footer | Replaced `bm_def`, `bm_def_lc`, `bm_boot`, and `bm_pairing` with reproducible pixel-font artwork from `Tools/oled_bitmaps.py` in `GraphicsStolo.h`; generated enrollment, rescue, new-identity and store-error banners plus countdown digits. Both idle variants say STOLO NODE; boot carries factual RNode firmware / unsigned.io / GPLv3 attribution wrapped onto two lines, and `Console/build.py` adds the same attribution in its page footer. The display reads a pure priority policy (PIN first), overrides external framebuffer/automatic blanking for notices, shows the 60 s presence window, and announces successful rescue for 5 s. HELLO's former attestation byte now reports advisory display/window capability bits, decoded by the host tool. The Stolo remote AP fallback becomes Stolo Node XXXX; configured SSIDs and the RNode XXXX Bluetooth name stay unchanged. OTA and console bitmaps stay unchanged. | F8 physical-window visibility and factual upstream attribution; software verified by host policy tests and firmware compilation, visuals await device photos. |

| 2026-09-21 | OLED review corrections | Preserve simultaneous hardware/firmware faults above the Stolo PIN or F8 notice; restore original lower-band error priority in plain builds and when no Stolo notice is active. Saturate countdowns at 99 before converting to a byte, keeping both digit indices within the generated table. Add host regressions for fault placement and long countdowns across timer wrap. | Review findings: persistent store errors must not hide a second fault, and future longer windows must not overrun digit artwork. Visual coexistence still awaits device photos. |
| 2026-09-21 | `BLESerial.cpp`, `Bluetooth.h`, `StoloBleTrace.h`, `Stolo.h`, `StoloProtocol.h`, `Display.h`, `RNode_Firmware.ino`, `Makefile` | Software verified: optional `firmware-stolo-tbeam_supreme-ble-trace` build records USB timestamps, pairing/auth state, bond count, SMP callbacks and auth failure, GATT disconnect reason, local disconnect requests, control reset and deferred protocol boundary handling. Callbacks enqueue bounded records for loop-side USB output, with sequence/drop accounting, radio suffix and display-path observations. Normal builds compile tracing out. No passkeys or application payloads in diagnostic records. | Instrument fresh iPhone pairing before changing behavior; device qualified: none until the iPhone qualification gates pass. |
| 2026-09-21 | `Display.h`, `Bluetooth.h`, host regressions | Software verified: after USB failure capture, render passkeys with positive digit glyphs matching F8’s dark pairing banner; keep an already-open pairing window/code through failed authentication and peer cancellation, without extending bonded deadlines or reopening explicit closes. Protocol/GATT permissions and SMP passkeys remain unchanged. | USB framebuffer exposed inverse digits on the dark banner; auth failure trace showed the window closing with zero bonds. Device qualified: none pending iPhone gates. |
| 2026-09-21 | `Display.h`, PIN pixel regressions, pairing qualification record | Software verified: doubled PIN glyph dimensions from 3×5 to 6×10 while keeping all six digits within the panel. Device qualified: fresh iPhone passkey pairing, connected link, app ready and Node settings confirmed on the preceding small-digit candidate; enlarged layout and remaining qualification gates pending. | Rameen confirmed successful pairing and requested larger digits. |
| 2026-09-22 | Qualification records, `Tools/ble_read_benchmark.py` | Device qualified: enlarged code readability, bonded/no-bond cold boot, timed physical pairing window, fresh-pair recovery, silent reconnect and a 60-second concurrent NUS/CTRL read baseline (3,936 valid replies per channel). Software verified: matching USB/app traces and read-only benchmark harness; original 600-second window restored after 120-second tests. | Complete the previously skipped iPhone pairing checks; the app connection was restored; physical photos remain outstanding. |
